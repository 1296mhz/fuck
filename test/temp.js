/**
 * Created by cshlovjah on 15.05.16.
 */
/*
 var isAuthenticatedAdmin = function isAuthenticatedAdmin(req, res, next) {
 if ( req.isAuthenticated() && req.user.role === 'administrator' ) { return next(); }
 return res.redirect('/');
 };

 router.get('/onlyAdmin', isAuthenticatedAdmin, function (req, res) {
 res.render('onlyAdmin', { user : req.user });
 });
 */